<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css"></link>
    <title>Document</title>
  </head>
  <body>
    <h1>Todo APP</h1>
    <a href="/v1/todo/new">Add a Todo</a>
    <h3>Your Todos</h3>
    <ul>
      <% todos.forEach((todo) => { %>
        <li><%= todo.name %> - <%= todo.category %> - <%= todo.state %>  <a href="/v1/todo/<%= todo._id %>/edit">Edit</a> </li>
      <% }); %>
    </ul>
  </body>
</html>
